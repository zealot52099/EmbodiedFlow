# 具身智能快速入门全链路手册

目标：只看这一份文档，就能基本理解具身智能中和 **模型训练、数据构造、效果评测、仿真与硬件通信** 相关的核心知识，并能把这些知识和你已有的多模态 Agent / GUI Agent / 数据闭环经验连接起来。

这份文档不是论文清单，而是一份解释型学习手册。它会反复回答四个问题：

```text
这个概念为什么存在？
它解决具身智能链路里的什么问题？
它和上下游模块是什么关系？
面试或方案讨论时应该怎么讲？
```

---

## 1. 先理解具身智能到底在做什么

“具身智能”这个词很容易被说得很玄，但工程上可以先把它理解成一句话：

```text
让一个智能系统通过真实身体感知世界、理解任务、规划步骤、执行动作，并从执行反馈中持续改进。
```

普通大模型主要处理文本、图像、音频这些“信息世界”的对象。具身智能多了一个关键差异：它要改变物理世界。机器人不只是回答“杯子在哪里”，还要真的把杯子拿起来、移动过去、递给人。这里面每一步都可能失败：识别错杯子、抓取点不稳、路径碰撞、夹爪滑落、用户移动了位置，或者机器人执行延迟导致动作落后于环境变化。

所以具身智能不是“VLM + 机械臂”这么简单，而是一条闭环链路：

```text
用户指令
  -> 多模态感知
  -> 任务理解
  -> 高层规划
  -> Skill / Policy 执行
  -> 运动规划与控制
  -> 硬件动作
  -> 传感器反馈
  -> 成功/失败判断
  -> 日志和轨迹沉淀
  -> 数据清洗、标注、训练、评测
  -> 模型迭代
```

如果用你熟悉的 GUI Agent 来类比，会非常直观：

```text
GUI Agent：
  用户说“给张三发微信”
  -> 看手机截图
  -> 理解当前页面
  -> 规划点击、输入、滑动
  -> 执行动作
  -> 再看截图
  -> 判断是否成功
  -> 失败后 Replan

Robot Agent：
  用户说“把红杯子递给我”
  -> 看桌面和机器人状态
  -> 理解杯子、用户、障碍物的位置
  -> 规划观察、抓取、移动、递交
  -> 执行机器人动作
  -> 再看传感器反馈
  -> 判断是否抓稳、是否递交成功
  -> 失败后 Replan
```

这两个系统的抽象结构几乎一样，差异在于机器人多了物理约束：碰撞、摩擦、重力、力控、标定误差、硬件延迟和安全风险。

---

## 2. 具身智能的岗位和技术分层

具身智能行业里不同岗位经常都叫“机器人算法”或“具身智能算法”，但实际工作差异很大。为了不被词汇绕晕，可以按系统层次拆开。

### 2.1 硬件本体层

这一层关心机器人身体本身，包括机械臂、灵巧手、移动底盘、电机、传感器、控制板、末端执行器等。

它回答的问题是：

```text
机器人有哪些自由度？
负载多大？
重复定位精度多少？
相机装在哪里？
夹爪能不能稳定抓住目标？
电机和控制器响应有多快？
```

这层更偏机械、电子、嵌入式和控制，不是你最应该优先切入的方向。你需要了解它对上层模型的约束，但不必把自己定位成硬件专家。

### 2.2 感知层

感知层负责把真实世界变成机器可用的状态表示。

例如，机器人看到一张桌面图像，上层模型不能只拿到“像素”。系统通常还需要知道：

```text
有哪些物体？
目标物体在哪里？
物体是否被遮挡？
目标是否可抓？
桌面和障碍物在哪里？
人手是否靠近危险区域？
```

典型技术包括目标检测、分割、深度估计、点云处理、6D Pose、SLAM、场景图、人体姿态、多模态 VQA 等。

你过去做过 OCR、检测、人脸、车内外 VQA、页面状态识别，这些都可以迁移到机器人感知和状态理解。需要补的是 3D 和坐标系：机器人不是只要知道“杯子在图像左侧”，还要知道“杯子在机器人 base 坐标系下的位置和姿态”。

### 2.3 数据层

机器人学习特别依赖数据，而且数据比普通图文数据更贵。

一张互联网图片只需要图文对；一条机器人训练数据通常需要：

```text
图像
深度或点云
机器人关节状态
末端执行器位姿
夹爪状态
动作
时间戳
任务指令
成功/失败标签
环境信息
```

这就是为什么“机器人数据引擎”“数据闭环”“自动评测”“badcase 挖掘”会成为非常重要的岗位方向。对你来说，这一层很有优势，因为你已经做过 Agent 日志、评测、数据回流、Run Store、Lineage、Audit、Gate/Eval 的设计。

### 2.4 模型层

模型层回答的问题是：

```text
机器人应该如何从视觉、语言和自身状态中预测下一步动作？
```

这里会出现 VLM、VLA、ACT、Diffusion Policy、模仿学习、offline RL、SFT、DPO、trajectory reward 等概念。它们不是一堆孤立名词，而是在解决同一个问题：如何让机器人从数据中学会可执行的行为。

### 2.5 Agent 层

Agent 层不是直接控制每个关节，而是做任务级编排。

例如用户说“帮我整理桌面”，Agent 需要判断：

```text
桌上有哪些东西？
哪些东西应该收纳？
先拿哪个？
放到哪里？
当前动作是否成功？
失败了该重试、换策略，还是问用户？
```

这层和你做过的 GUI Agent 最像。只不过 GUI 的工具是 click、swipe、type，机器人这里的工具是 pick、place、navigate、open、handover。

### 2.6 执行控制层

执行层负责把高层动作变成物理可执行轨迹。

例如 Agent 说 pick(red cup)，底层系统要解决：

```text
抓取点在哪里？
机械臂能不能到达？
路径会不会碰撞？
夹爪什么时候闭合？
抓取失败怎么检测？
```

这一层通常涉及 MoveIt、运动规划、IK、轨迹控制、力控、底盘导航、控制器等。你需要知道它们在系统里的位置，但不建议把求职主线放在最底层控制。

---

## 3. VLM、VLA、Policy、Agent 的关系

很多资料会同时讲 VLM、VLA、Policy、Agent，容易混在一起。最简单的区分方式是看它们的输入输出。

### 3.1 VLM：看懂世界，但不一定动手

VLM 是 Vision-Language Model，输入图像或视频加文本，输出文本或结构化理解结果。

它适合回答：

```text
桌上有什么？
红杯子在哪里？
用户指的是哪个物体？
这个场景有什么风险？
现在任务失败可能是什么原因？
```

在机器人系统中，VLM 常常充当“理解层”。它把视觉世界翻译成语义世界，让 Agent 或 Policy 能够使用这些信息。

但 VLM 本身不一定知道机器人怎么动。它可能能说“红杯子在桌子右侧”，但不知道机械臂该移动多少厘米、夹爪什么时候闭合。

### 3.2 VLA：从看懂到会动

VLA 是 Vision-Language-Action Model。它比 VLM 多了 Action。

普通 VLM 是：

```text
image + text -> text
```

VLA 是：

```text
image + text + robot_state -> action
```

这里的 robot_state 很关键。机器人不是漂浮的眼睛，它有身体状态。模型必须知道机械臂现在在哪里、夹爪是否打开、关节是否接近限位、末端执行器和目标物体的空间关系如何。

VLA 可以输出不同粒度的动作：

```text
低层连续动作：末端执行器移动一点，夹爪闭合一点。
中层动作序列：未来 10 步 action chunk。
高层 skill 调用：pick(red cup)、place(cup, tray)。
```

### 3.3 Policy：机器人行为函数

Policy 可以理解成机器人行为函数：

```text
action = policy(observation, instruction, robot_state)
```

Policy 不一定是大模型。它可以是一个小的 Transformer，也可以是 Diffusion Policy，也可以是 OpenVLA 或 π0 这样的 VLA。

在系统里，Policy 负责“在当前状态下应该做什么”。它更接近执行，而 Agent 更接近任务编排。

### 3.4 Agent：长程任务的组织者

Agent 负责把用户目标组织成多个可执行步骤，并根据反馈调整。

比如：

```text
用户：把桌上的红杯子递给我。

Agent 计划：
  1. 观察桌面
  2. 定位红杯子
  3. 判断是否可达
  4. 调用 pick(red cup)
  5. 检查是否抓稳
  6. 移动到递交位置
  7. 调用 handover(red cup)
  8. 检查用户是否接收
```

如果第 4 步失败，Agent 不应该继续盲目执行第 5 步，而应该重新观察、换抓取点、降低速度，或者询问用户。

这就是你熟悉的 Reflection/Replan 在机器人里的对应物。

---

## 4. 机器人“大脑”的三种架构路线

### 4.1 模块化路线

模块化路线把系统拆成多个明确模块：

```text
VLM 负责看懂
LLM/Agent 负责规划
Skill Library 负责执行
MoveIt/Controller 负责运动和控制
```

它的优点是可控、可解释、容易调试。产品落地时很常见，因为一旦失败，你能定位是感知错、规划错、skill 错，还是控制器错。

缺点是上限依赖手工设计的 skill。如果没有“拧瓶盖”这个 skill，Agent 再聪明也很难完成拧瓶盖。

### 4.2 端到端 VLA 路线

端到端 VLA 试图直接学习：

```text
视觉观察 + 语言指令 + 机器人状态 -> 动作
```

它的吸引力在于泛化潜力。只要有足够多跨任务、跨场景、跨机器人的数据，模型可能学到比手工 skill 更通用的能力。

问题也很明显：

```text
数据需求极大
失败原因难解释
真实机器人评测成本高
安全边界难保证
推理延迟和稳定性压力大
```

所以端到端 VLA 更像长期方向，不一定是所有产品的第一落地形态。

### 4.3 混合路线

更现实的路线通常是混合式：

```text
高层 Agent 负责长程任务规划
VLA / learned policy 负责局部动作
传统运动规划和控制器负责安全可执行
数据闭环负责持续改进
```

例如 pick 这个 skill 内部可以用 learned policy，但执行前后仍然有：

```text
precondition check
safety check
motion constraint
success checker
failure recovery
logging
```

这条路线最适合你切入，因为它需要 Agent 架构、工具调用、状态管理、评测、数据闭环和工程落地能力。

---

## 5. 模型训练：从会看图到会行动

### 5.1 为什么普通 VLM 不够

普通 VLM 能看懂图，也能做推理，但机器人需要的是“可执行行为”。这中间有一个巨大的鸿沟。

比如 VLM 看到桌上的杯子，能回答“红杯子在蓝碗左边”。但机器人要完成抓取，还需要知道：

```text
杯子的 3D 位置在哪里？
机器人末端执行器现在在哪里？
从哪个方向接近比较安全？
夹爪开口多大？
路径是否会撞到碗？
抓起来后杯子是否会滑落？
```

所以训练机器人“大脑”时，不能只做图文问答 SFT，还要让模型接触 observation-action trajectory。

### 5.2 机器人训练样本和普通 VLM 样本的区别

普通 VLM 样本像这样：

```json
{
  "image": "table.jpg",
  "question": "What is on the table?",
  "answer": "A red cup and a blue bowl."
}
```

机器人 VLA 样本更像这样：

```json
{
  "instruction": "pick up the red cup",
  "observation": {
    "rgb_static": "image_from_external_camera",
    "rgb_wrist": "image_from_wrist_camera",
    "robot_state": {
      "joint_positions": [0.1, 0.2, 0.3],
      "end_effector_pose": [0.42, 0.12, 0.28, 0.0, 1.57, 0.0],
      "gripper": "open"
    }
  },
  "action": {
    "delta_end_effector_pose": [0.01, -0.02, -0.03, 0.0, 0.0, 0.1],
    "gripper": "close"
  }
}
```

区别在于：机器人样本必须包含身体状态和动作。没有这些，模型只能“说”，不能“做”。

### 5.3 动作表示为什么重要

动作表示决定了模型到底学什么。

如果让模型输出文字计划：

```text
move to the cup, grasp it, lift it
```

这适合高层规划，但不能直接驱动电机。

如果让模型输出末端位姿增量：

```text
dx, dy, dz, droll, dpitch, dyaw, gripper
```

它更接近真实执行，但对数据精度、坐标系、控制器都有要求。

如果让模型输出 skill：

```json
{"skill": "pick", "object": "red_cup"}
```

它更可控，也更适合产品系统，但能力受限于已有 skill library。

因此动作空间不是一个细节，而是系统架构选择。面试时可以这样讲：

```text
如果目标是可控落地，我倾向于高层 Agent 输出 skill call，skill 内部由 learned policy 或传统 planner 执行。
如果目标是研究通用操作能力，则会关注连续动作或 action chunk 的 VLA / policy 训练。
```

### 5.4 模仿学习：机器人学习的冷启动方法

模仿学习的直觉很简单：让人先做给机器人看，机器人再学习人的行为。

人类通过遥操作完成任务，系统记录每一帧：

```text
看到了什么
机器人当时是什么状态
人类操作者给了什么动作
最后任务是否成功
```

然后训练模型学习：

```text
在类似观察下，输出类似专家动作。
```

这就是 Behavior Cloning。它的优点是简单、稳定、可复现，适合作为第一版 baseline。缺点是遇到没见过的状态容易漂移。比如专家示范里杯子从来没倒过，机器人真实执行时杯子倒了，模型可能不知道怎么恢复。

这和 GUI Agent 的轨迹学习非常像。GUI Agent 学的是：

```text
截图 + 用户目标 -> click / type / swipe
```

机器人学的是：

```text
视觉观察 + 用户目标 + 机器人状态 -> move / grasp / place
```

### 5.5 ACT：为什么要一次预测一段动作

如果 policy 每一帧只预测下一步动作，会有两个问题。第一，单步预测容易抖动；第二，长程任务里误差会逐步累积。

ACT，即 Action Chunking Transformer，核心思想是一次预测未来一小段动作。

```text
普通 policy：
  当前观察 -> 下一步动作

ACT：
  当前观察 -> 未来 k 步动作序列
```

这样做有点像 GUI Agent 不是只想“下一次点击”，而是直接生成“打开联系人 -> 搜索张三 -> 点击聊天框 -> 输入消息”的短轨迹。机器人里 action chunk 可以让动作更连贯，也降低每一步重新决策带来的不稳定。

ACT 适合用作快速 baseline，尤其是遥操作数据不算特别大、任务相对明确的桌面操作场景。

### 5.6 Diffusion Policy：为什么用扩散模型生成动作

机器人动作经常不是单一答案。例如抓一个杯子，可以从左边抓，也可以从右边抓；可以先绕开障碍，也可以换一个角度接近。传统回归模型容易把多个合理动作平均，结果生成一个“不像任何专家动作”的中间动作。

Diffusion Policy 的价值在于，它更适合建模这种多峰动作分布。它不是直接回归一个动作，而是在当前观察条件下，从噪声中逐步生成一段合理动作序列。

可以这样理解：

```text
VLM 扩散模型生成图像：从噪声中生成符合文本条件的图片。
Diffusion Policy 生成动作：从噪声中生成符合观察和任务条件的动作轨迹。
```

它适合复杂、精细、接触丰富的操作任务。代价是推理更重，部署时要考虑延迟、控制频率和动作平滑。

### 5.7 RT-2：把互联网知识迁移到机器人动作

RT-2 的核心启发是：机器人数据很少，但互联网图文知识很多。能不能让模型既学习网页级视觉语言知识，又学习机器人动作？

RT-2 的做法是把机器人动作表示成 token，使模型可以用类似语言生成的方式输出动作。这样 VLM 里学到的概念，例如“垃圾”“水果”“抽屉”“递给人”，就可能迁移到机器人任务中。

它的关键不是某个具体结构，而是这个范式：

```text
把语义理解和动作生成放进统一模型。
```

面试时可以这样讲：

```text
RT-2 说明 VLA 的重要方向是利用大规模 VLM 的语义知识，通过机器人动作数据把语义 grounding 到可执行行为上。
```

### 5.8 OpenVLA：开源 VLA 的参考范式

OpenVLA 是目前学习 VLA 最值得认真拆解的项目之一，因为它把“视觉语言模型如何变成机器人动作模型”这件事做成了开源模型、代码、训练管线和评测脚本。你可以把它当成 VLA 领域的一个标准样板：先理解它，就能理解很多后续 VLA 工作在改什么。

OpenVLA 的官方定位是一个 7B 参数开源 Vision-Language-Action 模型，基于 Open X-Embodiment 中约 970k 条真实机器人 episode 训练，目标是做通用机器人操作策略。它的关键价值不是“又一个 7B 模型”，而是证明了一个路线：

```text
先用大规模视觉语言模型获得语义理解能力
再用大规模机器人轨迹数据把语义 grounding 到动作
最后通过少量目标机器人数据微调到具体 embodiment 和任务
```

#### 5.8.1 OpenVLA 解决什么问题

传统机器人学习经常是“一个任务训练一个 policy”。比如训练一个模型专门把胡萝卜放进碗里，再训练另一个模型专门把杯子放到盘子上。这种方式在窄任务上可以很好，但遇到多物体、多语言指令、多场景泛化时会很吃力。

OpenVLA 想解决的是更通用的问题：

```text
机器人能否利用互联网 VLM 的语义能力？
能否用跨机器人、跨任务的数据训练一个通用操作基座？
能否在新机器人上用较少 demonstration 快速适配？
```

这和普通 VLM 的区别在于，OpenVLA 不只是回答“红杯子在哪里”，而是输出可以被机器人执行的动作。

```text
普通 VLM：
  图像 + 指令 -> 文本回答

OpenVLA：
  图像 + 指令 -> 动作 token -> 连续机器人动作
```

#### 5.8.2 原理：OpenVLA 的模型结构

vanilla OpenVLA 的模型可以拆成三部分。

第一部分是视觉编码器。OpenVLA 使用融合式视觉编码，结合 SigLIP 和 DINOv2 这样的预训练视觉 backbone。直观理解是：SigLIP 更偏图文对齐，DINOv2 更偏强视觉表征，二者融合后既有语义能力，也有较好的视觉细节表征。

第二部分是 projector。视觉编码器输出的是 image patch embedding，语言模型不能直接使用，所以需要 projector 把视觉特征映射到 LLM 的输入空间。

第三部分是 Llama 2 7B 语言模型 backbone。这里最关键的设计是：OpenVLA 让语言模型预测“动作 token”。动作 token 再被 decode 成连续动作，交给机器人执行。

整体可以理解为：

```text
图像
  -> SigLIP + DINOv2 视觉编码
  -> projector 映射到语言模型空间
  -> Llama 2 7B 生成动作 token
  -> decode 成连续动作
  -> robot controller 执行
```

这套设计的意义在于，它把 VLM 的生成式范式迁移到了动作生成上。模型不是只生成文字，而是生成离散化后的动作表示。

#### 5.8.3 动作表示：为什么 vanilla OpenVLA 用离散 token

机器人动作通常是连续值，例如末端执行器的位姿变化：

```text
dx, dy, dz, droll, dpitch, dyaw, gripper
```

但 LLM 擅长生成 token。OpenVLA 的做法是把连续动作离散化，例如把每个动作维度分桶，然后表示成 token。模型生成 token 后，再把 token 反解码成连续动作。

这样做的好处是：

```text
可以复用语言模型的自回归生成机制
训练目标接近 next-token prediction
视觉、语言、动作可以进入统一序列建模框架
```

缺点也很明显：

```text
动作精度受离散粒度限制
生成多个动作 token 会带来推理延迟
高频控制时速度不够理想
action chunk 表达不够高效
```

这就是为什么后续出现了 FAST action tokenizer 和 OpenVLA-OFT。vanilla OpenVLA 适合用来理解 VLA 基本范式，但如果你要做实际微调和部署，需要关注更新的 OFT 路线。

#### 5.8.4 OpenVLA-OFT：现在更实用的微调路线

OpenVLA-OFT 是 Optimized Fine-Tuning recipe。它不是完全另起炉灶，而是在 OpenVLA 的基础上解决几个实际问题：

```text
vanilla OpenVLA 推理慢
离散 action token 对高频控制不够友好
单图输入限制了复杂机器人场景
自回归动作生成不适合需要快速闭环的机器人
```

OFT 的核心变化包括：

```text
parallel decoding
action chunking
continuous action representation
L1 regression objective
支持多图输入和 proprioceptive state
```

直观理解是：vanilla OpenVLA 更像“LLM 逐个生成动作 token”，而 OFT 更像“利用 VLA 表征，一次回归出一段连续动作”。这会显著改善推理速度和机器人控制频率。

如果面试被问“OpenVLA 怎么用”，建议你不要只回答原始 LoRA fine-tuning，而要补一句：

```text
原始 OpenVLA 帮我们理解 VLA 的开放训练范式；实际做新任务适配时，现在更应该关注 OpenVLA-OFT，因为它把动作表示改成连续 action chunk，并通过并行解码和 L1 regression 提升速度和成功率。
```

#### 5.8.5 使用方法：从零到跑起来

OpenVLA 的使用可以分成四种层级。你不一定一开始就全做，学习时可以按难度逐步推进。

第一层是直接加载预训练模型做推理。它适合理解输入输出，不适合期望它在你的新机器人上 out-of-the-box 做得很好。OpenVLA 官方也提醒，模型通常需要在目标机器人域上用少量 demonstration 微调，才能稳定工作。

基本流程是：

```text
安装 openvla 仓库
加载 openvla/openvla-7b checkpoint
输入当前图像和语言指令
模型输出动作
把动作转换成机器人控制命令
在仿真或真实机器人上 rollout
```

第二层是 LoRA 微调。这是最常见的入门 fine-tuning 方式，因为不需要全量训练 7B 模型。官方 README 给出的例子是在 BridgeData V2 上用 LoRA 微调 `openvla/openvla-7b`，单张 80GB A100 可以跑，较小显存也可以通过减 batch size 和梯度累积处理。

LoRA 微调的思路是：

```text
冻结大部分基座参数
只训练低秩 adapter
用目标机器人数据适配视觉、语言和动作分布
```

第三层是 full fine-tuning。它会更新全部参数，适合目标域和预训练域差异很大、LoRA 不够用的情况。但成本高，官方示例提到使用多卡 A100 节点。这不是入门优先路径。

第四层是 OpenVLA-OFT 微调。对于真实机器人部署或高频控制，OFT 更值得关注。它通过连续动作、action chunking 和并行解码改善速度和成功率。学习时可以把它当成“OpenVLA 的实用进化版”。

可以按下面顺序动手：

```text
1. 先读 OpenVLA GitHub README，跑通环境安装。
2. 用 Hugging Face checkpoint 加载模型，理解输入输出。
3. 跑 LIBERO 或 BridgeData V2 的评测脚本，先不要接真实机器人。
4. 准备一个小的目标任务数据集，做 LoRA 或 OFT 微调。
5. 在仿真中 rollout，看 success rate 和 failure mode。
6. 如果有真实机器人，再接入 ROS2/控制器做小范围安全测试。
```

#### 5.8.6 数据：OpenVLA 需要什么格式

OpenVLA 的预训练数据来自 Open X-Embodiment。它不是普通图文数据，而是机器人 demonstration episode。每条 episode 都包含观察、动作、指令和机器人状态等信息。

一条用于 VLA 训练的数据，本质上是：

```text
当前视觉观察 + 当前语言指令 + 当前机器人状态 -> 专家动作
```

常见字段包括：

```text
外部相机图像
腕部相机图像
机器人 proprioceptive state
末端执行器位姿
夹爪状态
动作
自然语言指令
episode 成功/失败信息
```

OpenVLA 代码更偏向使用 RLDS 格式的数据管线。RLDS 可以理解成用于记录强化学习/机器人 episode 的标准数据格式，适合存储 trajectory。官方 README 里也建议，自定义数据优先转换成 RLDS，再注册 dataset config 和 transform。

如果你有自己的机器人数据，要关注三件事。

第一是 observation 对齐：

```text
图像、机器人状态、动作必须按时间戳对齐。
不能让 t 时刻图像配 t+3 时刻动作。
```

第二是 action space 对齐：

```text
你的机器人动作维度是什么？
是末端 delta pose，还是关节角，还是夹爪命令？
和 OpenVLA 的 transform 如何对应？
```

第三是语言指令质量：

```text
同一任务是否有多样化表达？
指令是否明确目标物体？
是否覆盖多物体、多目标、多场景？
```

OpenVLA 更适合多任务、多物体、强语言 grounding 的场景。如果是非常窄的单指令精密任务，Diffusion Policy 或 ACT 从头训练有时会更强。

#### 5.8.7 微调数据量该怎么理解

OpenVLA 的一个重要卖点是可以通过目标域小数据微调适配新机器人。但“小数据”不等于十几条随便采的轨迹就够了。

经验上要看任务复杂度：

```text
单一 pick/place：几十到一百条高质量 demo 可能可以做 baseline。
多物体语言条件任务：需要更多 demo 覆盖不同对象、位置和表达。
灵巧操作或双臂任务：数据需求会明显增加，还要覆盖失败和恢复。
```

数据质量通常比数量更重要。低质量 demonstration 会让模型学到坏习惯。OFT 项目里也特别提到，L1 regression 在某些真实数据噪声场景中反而可能比 diffusion 更稳，因为它不会过度复现 demonstration 里的次优动作模式。但这不是说 L1 永远优于 diffusion，而是说明真实机器人数据质量会强烈影响算法选择。

#### 5.8.8 测试：OpenVLA 怎么评估

OpenVLA 的测试不能只看训练 loss 或 action token accuracy。最终要看 rollout。

测试可以分四层。

第一层是离线 sanity check：

```text
回放 demonstration，确认你的机器人执行专家动作能够成功。
用训练集图像喂给模型，检查 action accuracy 或 L1 error 是否接近训练日志。
确认数据 transform、归一化和反归一化没有错。
```

这一步非常关键。很多 VLA 失败不是模型不行，而是数据坐标、动作归一化、相机输入或 control adapter 对不上。

第二层是仿真 benchmark，例如 LIBERO。OpenVLA 仓库提供了 LIBERO 相关评测入口，并有针对 LIBERO-Spatial、LIBERO-Object、LIBERO-Goal、LIBERO-10 的 checkpoint。LIBERO 适合在没有真实机器人时快速理解 VLA evaluation。

第三层是真实机器人 rollout。OpenVLA 原项目在 Bridge V2 WidowX、Google Robot、Franka 等平台上做了评测。真实测试要记录：

```text
任务成功率
完成时间
动作步数
失败原因
是否碰撞
是否能从失败中恢复
语言 grounding 是否正确
```

第四层是泛化测试。OpenVLA 官方特别关注多类泛化：

```text
视觉泛化：新背景、干扰物、颜色、外观。
运动泛化：新位置、新朝向。
物理泛化：新尺寸、新形状。
语义泛化：新对象、新指令、新互联网概念。
```

这也是 VLA 相对传统 policy 的重点价值：不仅要会重复训练任务，还要能根据语言和视觉理解处理新组合。

#### 5.8.9 常见失败和排查

OpenVLA 微调后效果不好，通常先不要急着换模型，而要查管线。

常见问题包括：

```text
相机视角和训练数据不一致。
图像预处理和训练时不同。
action normalization / unnormalization 错误。
机器人控制频率和训练数据频率不一致。
夹爪命令语义反了，例如 open/close 编码相反。
数据时间戳没有对齐。
语言指令太单一，导致 grounding 弱。
目标域和 OpenX 预训练分布差异过大。
只看离线 loss，没有做真实 rollout。
```

一个好的排查顺序是：

```text
1. 先确认 demonstration replay 能成功。
2. 再确认模型在训练样本上能复现动作。
3. 再确认 action adapter 输出到机器人控制器的动作方向和尺度正确。
4. 再做短 horizon rollout。
5. 最后做完整任务和泛化评测。
```

这和 GUI Agent 排查很像：先确认轨迹数据对，再确认模型输出对，再确认执行链路对，最后看端到端任务成功率。

#### 5.8.10 OpenVLA 适合什么、不适合什么

OpenVLA 适合：

```text
多任务机器人操作
多物体语言条件任务
需要语言 grounding 的 manipulation
已有少量目标机器人 demonstration，希望快速微调
想研究通用 VLA 训练和评测范式
```

OpenVLA 不一定适合：

```text
极窄但高精度的单任务控制
对高频实时控制要求极高但没有 OFT 优化
完全没有目标机器人 demonstration 的新场景
底层力控、动力学控制或安全关键实时控制
```

面试时可以这样总结：

```text
OpenVLA 是理解 VLA 的关键开源范式。它用预训练 VLM 表征加机器人 demonstration，把视觉语言理解迁移到动作生成。原始 OpenVLA 使用离散 action token 和自回归生成，适合理解统一建模；实际新机器人适配时，要关注 LoRA、RLDS 数据管线、rollout 评测，以及 OpenVLA-OFT 这种连续 action chunk 的优化路线。
```

### 5.9 π0 / OpenPI：更接近通用机器人策略

π0 系列强调 generalist robot policy，也就是用一个更通用的策略模型处理多种机器人任务。它比单纯 VQA 更接近真实操作，比传统固定 skill 更强调泛化。

学习 π0 时不要只记名字，而要关注它背后的趋势：

```text
机器人 policy 正在从小任务小模型，走向大规模数据驱动的通用策略。
模型不仅要理解语言和视觉，还要生成高频、连续、稳定的动作。
```

这也是为什么后续岗位会同时问 VLM、VLA、Diffusion Policy、ACT、数据集和 rollout。

### 5.10 后训练：SFT、DPO、RL、offline RL 在机器人里的含义

在普通 LLM 里，SFT 是让模型学会按指令回答。机器人里，SFT 可以有多种形式：

```text
instruction -> plan
observation -> action
observation + instruction -> skill call
failure state -> recovery action
trajectory history -> next action
```

DPO 或偏好优化也可以迁移到机器人。偏好对不一定是两个文本回答，也可以是两条轨迹：

```text
成功轨迹 > 失败轨迹
短且安全的轨迹 > 绕远且危险的轨迹
会询问澄清的轨迹 > 不确定还强行动作的轨迹
能恢复的轨迹 > 失败后继续漂移的轨迹
```

强化学习在真实机器人上比较贵，因为在线试错会损耗硬件并带来安全风险。所以更常见的是：

```text
仿真中 RL
offline RL
小规模真实环境安全探索
reward model 辅助优化
```

trajectory reward 很重要，因为机器人任务的质量不是单步动作决定的，而是整段执行过程决定的。

---

## 6. 数据构造：机器人模型真正吃什么

### 6.1 为什么机器人数据比图文数据难

机器人数据难，不是因为存储格式复杂，而是因为每条数据都来自真实或仿真的行动过程。

互联网图文数据可以静态采集，而机器人数据要回答：

```text
机器人当时看到了什么？
身体处于什么状态？
执行了什么动作？
动作有没有真的生效？
任务最后成功了吗？
如果失败，是感知错、规划错、控制错，还是硬件问题？
```

这意味着机器人数据天然是时序数据、闭环数据、因果数据。单帧图像不够，必须有 trajectory。

### 6.2 Episode：机器人数据的基本单位

一条 episode 是一次完整任务尝试，从开始到成功、失败、超时或人工中止。

它通常包含：

```text
任务指令
多路相机图像
深度或点云
机器人关节状态
末端执行器位姿
夹爪状态
动作序列
时间戳
成功/失败标签
失败原因
环境元信息
```

为什么要按 episode 组织？因为机器人任务有过程。只看某一步，很难判断策略好坏。比如机器人抓杯子的第一步稍微绕远一点，也许是为了避开障碍；单步看不高效，整段看却更安全。

### 6.3 数据来源：遥操作、仿真、自主采集、互联网视频

遥操作是最直接的数据来源。人类操作者控制机器人完成任务，系统记录轨迹。它的优点是动作质量高、真实可靠，缺点是贵、慢、规模有限。

仿真数据适合大规模生成和安全测试。你可以在 Isaac Sim、MuJoCo、Gazebo 里随机化物体位置、光照、材质和任务条件，生成大量训练或评测样本。缺点是仿真和真实世界永远有 gap，尤其是接触、摩擦、材质、传感器噪声。

机器人自主采集适合闭环迭代。部署一个初版 policy 后，让机器人在真实环境中反复执行，收集成功和失败轨迹，再用这些数据改进模型。这和线上 Agent badcase 回流非常相似。

互联网视频提供丰富的人类行为先验，但缺少机器人动作标签。它更适合学习任务语义、物体交互和步骤结构，不容易直接变成低层控制数据。

### 6.4 标注不是只标答案，而是标过程

机器人标注要比 VQA 更细。除了“成功/失败”，还要知道为什么。

常见标注层次：

```text
任务级：任务类型、指令、成功/失败。
步骤级：approach、grasp、lift、move、place。
对象级：目标物体、干扰物、bbox、mask、pose。
状态级：是否抓稳、是否接触、是否放到目标区域。
失败级：目标丢失、抓取滑落、碰撞、IK 失败、误解指令。
偏好级：轨迹 A 是否比轨迹 B 更安全、更自然、更高效。
```

这些标注服务于不同训练目标。步骤标注可以训练 planner；动作标注可以训练 policy；失败标注可以训练 recovery；偏好标注可以做 DPO 或 reward model。

### 6.5 数据清洗的核心是对齐

机器人数据最容易出问题的是不同模态没有对齐。

比如：

```text
图像是 t 时刻的，action 是 t+1 时刻的。
相机时间戳和机器人状态时间戳不同步。
夹爪状态记录延迟。
任务已经失败，但标签仍然写成功。
不同机器人坐标系单位不一致。
```

所以数据清洗不是简单去重，而是确保 observation、state、action、label 在时间和语义上对得上。

### 6.6 数据闭环是机器人公司的核心资产

成熟的具身智能团队通常会建设这样的闭环：

```text
真实执行
  -> 自动记录视频、状态、动作、日志
  -> 自动判断是否成功
  -> 聚类高频失败
  -> 人工或模型辅助标注
  -> 生成训练集、验证集、回归集
  -> 训练新模型
  -> 仿真和真实评测
  -> 灰度部署
  -> 新一轮数据回流
```

这部分和你的 Data Agent 背景高度匹配。你可以把自己的经验表达成：

```text
我关注的不只是训练一个模型，而是建设从执行日志、轨迹数据、失败归因、评测回归到模型迭代的可治理闭环。
```

---

## 7. Skill 抽象：让大模型可控地控制机器人

### 7.1 为什么不能只靠大模型直接输出动作

如果让大模型直接输出所有底层动作，系统会很难控制。机器人动作有实时性、安全性和物理可执行性要求。一个错误点击可能只是发错消息，一个错误机械臂动作可能碰到人或打翻物体。

因此工程上常把动作分层：

```text
高层任务：整理桌面
中层 skill：pick、place、open、navigate
低层 policy：生成连续动作
控制器：执行轨迹和电机控制
```

Skill 是大模型和硬件之间的安全缓冲层。

### 7.2 一个好 Skill 应该长什么样

一个 skill 不只是一个函数名，而应该包含：

```text
它能做什么
需要哪些参数
执行前条件是什么
如何执行
如何判断成功
可能失败的原因
失败后如何恢复
是否有安全限制
执行日志怎么记录
```

例如 pick(red cup)：

```text
功能：抓起目标物体。
参数：object_id、目标位姿、抓取点候选。
前置条件：物体可见、可达、夹爪为空。
执行方式：调用 grasp policy 或 MoveIt 规划。
成功判定：夹爪闭合后物体随末端移动。
失败模式：目标丢失、抓取滑落、路径碰撞、IK 不可解。
恢复策略：重新观察、换抓取点、降低速度、请求用户移开障碍。
```

这和你做 GUI Agent 时对 action 的约束很像。click 不是随便点，必须知道目标元素、页面状态、点击后预期变化和失败校验。

### 7.3 Skill 和 VLA 的关系

Skill 不一定是规则写死的。它内部可以调用 VLA 或 learned policy。

一种常见结构是：

```text
Agent 输出：pick(red cup)
Skill 负责：检查前置条件、准备局部任务
VLA/Policy：生成抓取过程中的动作
Motion Planner：保证轨迹安全
Monitor：判断是否成功
```

这样既利用了学习模型的泛化能力，又保留了工程系统的可控性。

---

## 8. 效果评测：机器人模型好不好不能只看准确率

### 8.1 为什么 success rate 还不够

任务成功率当然重要，但它不是全部。

假设两个机器人都能把杯子递给用户：

```text
机器人 A：5 秒完成，动作平稳，没有碰撞。
机器人 B：30 秒完成，中间差点撞到碗，夹爪抖动，最后勉强成功。
```

它们的 success rate 都是成功，但产品体验和安全性完全不同。

所以具身评测必须同时看结果、过程、安全、泛化和恢复。

### 8.2 核心指标怎么理解

Task Success Rate 衡量任务是否完成。它是第一指标，但要配合测试次数和任务分布。只测 10 次成功 9 次，不等于模型稳定可上线。

Step Efficiency 衡量是否高效。机器人可能完成任务，但绕远路、反复观察、重复抓取，这会影响体验和能耗。

Collision / Safety 衡量安全。任何碰撞、越界、急停、危险接近都要单独统计。机器人系统里安全指标通常比平均成功率更有优先级。

Recovery Rate 衡量失败后是否能恢复。真实环境中失败不可避免，关键是系统能否识别失败并重新规划。

Generalization 衡量泛化。训练时见过红杯子不够，测试时要换物体、换位置、换光照、换背景、换说法，甚至换机器人。

Long-horizon Stability 衡量长任务稳定性。单步 pick 成功不代表“收拾桌面”成功，因为长任务会产生错误累积。

Human Preference 衡量人类体验。动作是否自然、是否让人安心、该问的时候是否会问、不该打扰时是否安静，这些很难只用自动指标覆盖。

### 8.3 评测集应该覆盖什么

一个靠谱评测集不能只测最简单场景。它应该包含：

```text
基础操作：pick、place、open、close、handover。
组合任务：取物递交、整理桌面、开柜取物。
长程任务：准备饮品、收纳多个物品。
干扰场景：遮挡、相似物、光照变化、目标位置变化。
语言变化：省略、指代、多轮修改、模糊指令。
安全场景：人手靠近、易碎物、禁区、危险物。
恢复场景：抓取滑落、目标丢失、路径失败、识别不确定。
```

评测集的本质是定义产品能力边界。你测什么，团队就会优化什么。

### 8.4 失败归因比失败率更重要

如果只知道“失败了”，模型很难迭代。你需要知道失败发生在哪一层。

常见归因：

```text
感知失败：没找到目标、pose 错、分割错。
语言失败：误解指令、指代错。
规划失败：步骤顺序错、忘记验证。
Skill 失败：抓取失败、放置失败、导航失败。
控制失败：IK 不可解、轨迹碰撞、执行抖动。
硬件失败：夹爪异常、相机掉帧、通信中断。
安全失败：碰撞、越界、接近人。
交互失败：该询问时没问，不该问时频繁问。
```

这部分是你很能发挥的地方。你可以把 Agent badcase 分析经验迁移成机器人 failure taxonomy 和 eval dashboard。

### 8.5 一个负责人级评测方案应包含什么

你可以按下面模板组织：

```text
1. 任务集：覆盖基础任务、组合任务、长程任务、恢复任务。
2. 数据切分：seen / unseen object、seen / unseen scene、seen / unseen instruction。
3. 指标体系：成功率、效率、安全、恢复、泛化、人类偏好。
4. 统计方法：每类任务足够 rollout 次数，报告置信区间。
5. 失败归因：按感知、规划、skill、控制、硬件、安全分类。
6. Gate 标准：达到阈值才允许上线或灰度。
7. 回流机制：失败 case 自动进入标注、训练和回归集。
```

---

## 9. 仿真、遥操作、Rollout 和 Sim-to-real

### 9.1 为什么需要仿真

真实机器人测试很贵。每次失败都可能浪费时间、损耗硬件，甚至产生安全风险。仿真环境让团队可以低成本、大规模地训练和测试。

仿真器可以用来：

```text
生成数据
训练 policy
测试任务
复现 badcase
验证安全策略
做大规模 rollout
```

常见工具：

```text
Isaac Sim / Isaac Lab：高保真仿真和机器人学习。
MuJoCo：轻量、高效，常用于控制和强化学习。
Gazebo：ROS 生态常用仿真器，适合系统集成。
RLBench / LIBERO / CALVIN：常用于机器人学习 benchmark。
```

### 9.2 遥操作为什么重要

机器人不会凭空学会动作。遥操作提供专家示范，是模仿学习最直接的数据来源。

典型流程是：

```text
定义任务
操作者远程控制机器人完成任务
系统记录图像、状态、动作、时间戳
标注任务成功或失败
清洗异常数据
训练 policy
在仿真或真实机器人上 rollout
```

你可以把遥操作理解成机器人版“人工演示轨迹”。GUI Agent 里可能有人类操作 App 的轨迹，机器人里就是人类操作机械臂的轨迹。

### 9.3 Rollout 是训练和评测之间的桥

训练 loss 下降不代表机器人真的会干活。必须让 policy 在环境中实际执行，这就是 rollout。

rollout 记录的是：

```text
每一步看到了什么
模型输出了什么动作
硬件实际执行了什么
执行后状态如何变化
最终成功还是失败
失败发生在哪一步
```

rollout 的意义在于暴露闭环错误。离线测试可能看起来不错，但真实执行时，一个小动作偏差会把模型带到训练数据没见过的状态，后面就可能一路漂移。

### 9.4 Sim-to-real 的核心矛盾

仿真再好，也不等于真实世界。

sim-to-real gap 来自：

```text
光照和材质差异
相机噪声
深度误差
摩擦系数不准
接触动力学不准
物体质量和形状误差
执行器延迟
标定误差
```

常见缓解方法：

```text
domain randomization：在仿真中随机化光照、材质、位置、摩擦。
真实数据 fine-tuning：用少量真实轨迹校准模型。
系统辨识：估计真实机器人的物理参数。
安全过滤：真实执行前约束动作范围。
混合训练：仿真数据提供覆盖，真实数据提供校准。
```

面试里不要把 sim-to-real 说成一个单点算法，它是数据、仿真、控制、感知和评测共同组成的工程问题。

---

## 10. 和硬件通信：ROS2、MoveIt、控制器

### 10.1 ROS2 是机器人系统的通信骨架

ROS2 不是训练框架，也不是大模型框架。它解决的是机器人系统里多个模块如何通信、启动、记录和协同。

一个机器人系统里可能有很多模块：

```text
相机驱动
深度传感器
关节状态发布器
VLA 推理节点
任务规划节点
运动规划节点
夹爪控制节点
安全监控节点
日志记录节点
```

ROS2 让这些模块通过标准方式通信。

### 10.2 Topic、Service、Action 怎么区分

Topic 适合持续流数据。例如相机图像、关节状态、底盘速度。

```text
/camera/image_raw
/joint_states
/cmd_vel
/tf
```

Service 适合一次性请求。例如查询当前模式、设置参数、请求一次识别。

Action 适合长时间任务。例如导航到某个位置、执行抓取、移动机械臂到某个位姿。Action 有 goal、feedback、result，还可以 cancel，所以机器人 skill 很适合用 Action 表示。

可以这样记：

```text
Topic：持续广播。
Service：问一次，答一次。
Action：交给你一个任务，执行中告诉我进度，最后给结果。
```

### 10.3 TF：机器人为什么特别关心坐标系

机器人抓物体不是看图说话。它必须把图像里的物体位置变成机器人坐标系下的可执行目标。

常见坐标系有：

```text
world
map
base_link
camera_link
end_effector
object_frame
```

假设相机看到杯子在图像中心，机器人还需要知道：

```text
杯子相对相机在哪里？
相机相对机器人底座在哪里？
机械臂末端相对底座在哪里？
目标抓取位姿是否可达？
```

TF 就是维护这些坐标变换的系统。对 VLA/Agent 来说，TF 是从“看见目标”到“能执行动作”的关键桥梁。

### 10.4 MoveIt：机械臂运动规划

MoveIt 解决的是“机械臂怎么安全地动过去”。

上层给它一个目标位姿：

```text
把末端执行器移动到杯子上方 10cm。
```

MoveIt 会尝试：

```text
求逆运动学
检查关节限制
检查路径碰撞
生成关节轨迹
发送给控制器执行
```

所以 MoveIt 不是大脑，而像一个运动规划专家。大脑决定“去抓杯子”，MoveIt 负责“怎么过去不撞东西”。

### 10.5 VLA 如何接到真实机器人

典型工程链路是：

```text
Camera Node 发布图像
Robot State Node 发布关节状态
VLA Inference Node 订阅 observation
VLA 输出 action 或 action chunk
Action Adapter 转成控制命令
Safety Checker 检查速度、边界、碰撞
Controller 执行
Monitor 观察结果
Logger 记录轨迹
```

这里有一个关键工程点：模型输出不等于硬件命令。中间通常需要 action adapter，把模型动作转换成机器人平台能理解的格式。

例如模型输出：

```text
delta_end_effector_pose = [0.01, -0.02, -0.03, 0, 0, 0.1]
gripper = close
```

系统要把它转换成：

```text
目标末端位姿
规划后的关节轨迹
控制器命令
夹爪命令
```

### 10.6 延迟和频率

机器人系统里不同层的频率不同。

```text
高层 Agent：低频，按任务步骤决策。
VLA / Policy：中低频，输出 action chunk。
轨迹控制器：中高频，执行平滑轨迹。
底层电机控制：高频，通常不由大模型直接控制。
```

大模型通常不适合直接做高频底层控制，因为推理延迟和稳定性不够。更合理的是：

```text
大模型低频输出计划或动作段
本地 policy/controller 高频执行
安全监控持续运行
```

这和车载端侧 Agent 的低延迟链路类似：你需要拆解采集、推理、传输、执行、反馈每一段耗时。

---

## 11. 安全与治理：机器人 Agent 必须可控

GUI Agent 犯错可能点错按钮，机器人 Agent 犯错可能造成物理风险。因此安全必须分层设计。

### 11.1 安全不是一个模块，而是一组边界

安全应该出现在每一层：

```text
任务层：拒绝危险任务。
规划层：高风险步骤需要确认。
Skill 层：检查 precondition。
运动规划层：检查碰撞和可达性。
控制层：限制速度、力和工作空间。
硬件层：急停、限位、保护机制。
日志层：所有高风险动作可追溯。
```

### 11.2 不确定时应该问，而不是乱动

机器人场景里，“询问用户”本身是一个重要 action。

例如：

```text
桌上有两个红杯子。
目标物体被遮挡。
用户要求拿危险物品。
机器人不确定用户手的位置。
```

这时一个成熟 Agent 应该能说：

```text
我看到两个红色杯子，你要左边那个还是右边那个？
```

这和 GUI Agent 的澄清机制一致，但机器人场景中它更重要，因为盲目执行成本更高。

---

## 12. 一个完整例子：把红杯子递给用户

下面用一个任务把全链路串起来。

用户说：

```text
把桌上的红色杯子递给我。
```

第一步是感知。相机和深度传感器采集桌面，感知模块识别红杯子、桌面、障碍物和用户位置。VLM 可以辅助判断“红杯子”是哪一个，并理解用户的递交意图。

第二步是规划。Agent 不应该直接输出关节角，而是拆成：

```text
observe(table)
locate(red cup)
check_reachable(red cup)
pick(red cup)
verify_grasp(red cup)
move_to_handover_pose(user)
handover(red cup)
verify_user_received
```

第三步是执行。pick skill 会检查杯子是否可见、夹爪是否为空、路径是否可达。然后它可能调用 VLA policy 生成局部动作，也可能调用 grasp planner 和 MoveIt 生成轨迹。

第四步是反馈。抓取后系统要判断杯子是否真的被夹住。如果夹爪闭合但杯子没动，说明抓取失败。Agent 应该重新观察、换抓取点或请求用户协助，而不是继续递交一个并不存在的杯子。

第五步是评测和回流。系统记录这次 episode，包括图像、状态、动作、成功/失败、失败原因和耗时。如果失败，它会进入 badcase 池，用于后续标注、训练和回归评测。

这个例子体现了具身智能的核心：不是单次模型输出，而是闭环执行。

---

## 13. 你应该如何学习和复述

### 13.1 最短学习路径

如果时间有限，按这个顺序学：

```text
1. 先看 LeRobot 教程，理解机器人学习闭环。
2. 看 RT-2、OpenVLA、π0，理解 VLA 的主线。
3. 看 Open X-Embodiment，理解机器人数据长什么样。
4. 看 ROS2 和 MoveIt，理解模型如何接硬件。
5. 看机器人评测资料，建立 success/safety/recovery/generalization 指标体系。
```

### 13.2 面试时的总括表达

你可以这样讲：

```text
我理解具身智能不是单点模型，而是从多模态感知、任务规划、动作生成、硬件执行、反馈监控到数据回流的闭环系统。

VLM 负责理解世界，VLA/Policy 负责把观察和指令转成动作，Agent 负责长程任务规划和失败恢复，ROS2/MoveIt/Controller 负责和真实机器人通信并保证动作可执行。

我过去在 GUI Agent 和车载多模态里做过状态理解、action trajectory、Reflection/Replan、评测体系和 badcase 回流，这些能力可以迁移到 Robot Agent，只是 action space 从 click/type/swipe 变成 pick/place/navigate/open/handover，并且需要额外考虑物理约束、安全和 sim-to-real。
```

---

## 14. 关键资料索引

### 14.1 总览和综述

- VLA Survey: https://vla-survey.github.io/
- VLA Models Explained: https://www.roboticscenter.ai/blog/vla-models-explained
- Foundation Models for Robotics VLA Blog: https://rohitbandaru.github.io/blog/Foundation-Models-for-Robotics-VLA/
- Robotics in the Era of Foundation Models: https://agentic.substack.com/p/robotics-in-the-era-of-foundation

### 14.2 VLA 与机器人学习

- LeRobot Robot Learning Tutorial: https://huggingface.co/spaces/lerobot/robot-learning-tutorial
- LeRobot GitHub: https://github.com/huggingface/lerobot
- RT-2 Official: https://robotics-transformer2.github.io/
- RT-2 DeepMind Blog: https://deepmind.google/blog/rt-2-new-model-translates-vision-and-language-into-action/
- OpenVLA Official: https://openvla.github.io/
- OpenVLA GitHub: https://github.com/openvla/openvla
- π0 Blog: https://www.pi.website/blog/pi0
- OpenPI GitHub: https://github.com/Physical-Intelligence/openpi
- Diffusion Policy: https://diffusion-policy.cs.columbia.edu/
- MIT Underactuated Robotics - Imitation Learning: https://underactuated.mit.edu/imitation.html

### 14.3 数据集

- Open X-Embodiment: https://robotics-transformer-x.github.io/
- DROID Dataset: https://droid-dataset.github.io/
- BridgeData V2: https://rail-berkeley.github.io/bridgedata/
- LIBERO: https://libero-project.github.io/
- CALVIN: https://calvin.cs.uni-freiburg.de/

### 14.4 机器人基础栈

- ROS2 Concepts: https://docs.ros.org/en/humble/Concepts/Basic.html
- ROS2 Tutorials: https://docs.ros.org/en/humble/Tutorials.html
- MoveIt 2 Documentation: https://moveit.picknik.ai/
- MoveIt Task Constructor Pick and Place: https://moveit.picknik.ai/main/doc/tutorials/pick_and_place_with_moveit_task_constructor/pick_and_place_with_moveit_task_constructor.html
- Isaac Lab: https://isaac-sim.github.io/IsaacLab/
- Isaac Lab Teleoperation and Imitation Learning: https://isaac-sim.github.io/IsaacLab/main/source/overview/imitation-learning/teleop_imitation.html
- MuJoCo Documentation: https://mujoco.readthedocs.io/
- Gazebo ROS2 Integration: https://gazebosim.org/docs/latest/ros2_integration/

### 14.5 评测

- NVIDIA Robot Policy Evaluation: https://developer.nvidia.com/blog/how-to-evaluate-general-purpose-robot-policies-for-real-world-deployment/
- RoboEval: https://robo-eval.github.io/
- Robot Learning Evaluation on Real Hardware: https://www.trossenrobotics.com/post/robot-learning-evaluation-benchmarking-real-hardware
